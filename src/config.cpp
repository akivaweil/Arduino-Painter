// config.cpp
#include "config.h"

// Define the global variables for motor configuration
int X_SPEED = 1000;         // Steps per second
int Y_SPEED = 1000;         // Steps per second
int ROTATION_SPEED = 1000;  // Steps per second
int X_ACCEL = 5000;         // Steps per second per second
int Y_ACCEL = 5000;         // Steps per second per second
int ROTATION_ACCEL = 500;   // Steps per second per second