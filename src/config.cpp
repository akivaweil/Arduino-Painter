// config.cpp
#include "config.h"

// Define the global variables for motor configuration
int X_SPEED = 2000;        // Steps per second
int Y_SPEED = 2000;        // Steps per second
int ROTATION_SPEED = 150;  // Steps per second
int X_ACCEL = 5000;        // Steps per second per second
int Y_ACCEL = 5000;        // Steps per second per second
int ROTATION_ACCEL = 200;  // Steps per second per second
int HOMING_SPEED = 700;    // Steps per seconds
int ROTATION_HOMING_SPEED = 700;